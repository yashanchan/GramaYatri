package com.transit.gramayatri.ui.theme

import androidx.compose.ui.graphics.Color

// Brand orange — used for active bottom nav icons, ETA pills, bus icon on timeline
val BrandOrange      = Color(0xFFD84315)
val BrandOrangeLight = Color(0xFFFF7043)
val BrandOrangeBg    = Color(0xFFFBE9E7)   // very light orange background for banners

// Stop timeline colours
val StopPassed       = Color(0xFFD84315)   // orange  — bus already passed
val StopCurrent      = Color(0xFFD84315)   // orange  — bus icon stop
val StopUpcoming     = Color(0xFFBDBDBD)   // grey    — not yet reached
val TimelineLine     = Color(0xFFBDBDBD)   // grey line connecting dots

// General UI
val SurfaceGray      = Color(0xFFF5F5F5)   // screen background
val CardWhite        = Color(0xFFFFFFFF)
val TextPrimary      = Color(0xFF212121)
val TextSecondary    = Color(0xFF757575)
val TextHint         = Color(0xFFBDBDBD)
val DividerColor     = Color(0xFFEEEEEE)

// ETA pill — green background for "Next in ~8 min"
val EtaPillGreen     = Color(0xFFE8F5E9)
val EtaPillGreenText = Color(0xFF2E7D32)

// Cancellation red
val CancelRed        = Color(0xFFD32F2F)
val CancelRedLight   = Color(0xFFFFEBEE)

// Avatar background
val AvatarOrange     = Color(0xFFBF360C)